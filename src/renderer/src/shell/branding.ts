/**
 * ✏️ ข้อความตรงกลางแถบด้านบน (top bar) — แก้ได้ที่ไฟล์นี้ที่เดียว
 *
 * แก้เสร็จแล้วถ้ารัน `npm run dev` อยู่ หน้าจอจะอัปเดตให้เอง
 * (ถ้าจะ build แจกจริงค่อยสั่ง `npm run build` อีกที)
 */

/** บรรทัดบน — ชื่อระบบ */
export const BRAND_NAME = 'A.E.R.I.S'

/** บรรทัดล่าง — คำอธิบายสั้น ๆ ใส่ข้อความที่ต้องการตรงนี้ */
export const BRAND_TAGLINE = 'ระบบตรวจวัดฝุ่น PM2.5 และการสวมหน้ากาก'
